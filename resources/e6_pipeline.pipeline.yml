resources:
  pipelines:
    e6_cdc_scd:
      name: "[${var.catalog}] E6 CDC + SCD (SDP)"
      serverless: true
      catalog: ${var.catalog}
      schema: wksp_scott_johnson_databricks_com   # per-person target (SA test value)
      libraries:
        - file:
            path: ../src/engineer/sdp/e6_cdc_scd_sdp.py
      configuration:
        wksp_schema: ${var.catalog}.wksp_scott_johnson_databricks_com
